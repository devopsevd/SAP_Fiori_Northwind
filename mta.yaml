_schema-version: "2.0.0"
ID: "Products.mta.rm"
version: 0.1.0

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "Products_mta"
    type: com.sap.hcp.html5
    path: .
    parameters:
       version: 0.1.0-${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist